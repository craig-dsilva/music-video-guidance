# Client

## Prerequisites

- Node.js (see `.nvmrc`)

## Setup

1. **Install dependencies**

   ```bash
   npm install
   ```

2. **Run the dev server**

   ```bash
   npm run dev
   ```

   This starts the application on http://localhost:3000.

